fastapi==0.115.0
uvicorn[standard]==0.32.0
jinja2==3.1.4
python-multipart==0.0.12
Pillow==10.4.0
pillow-heif==0.18.0
watchdog==5.0.3
markdown==3.7
# vision (off unless VISION=1): the libraries are small and always here, the
# ~225 MB of model is fetched only when it is switched on -- aperture/vision.py
onnxruntime==1.30.0
tokenizers==0.23.2
numpy==2.5.0
